class UserSource {
  final int id;
  final String name;

  UserSource({
    required this.id,
    required this.name,
  });
}

class UserTarget {
  final int id;
  final String name;

  UserTarget({
    required this.id,
    required this.name,
  });
}
