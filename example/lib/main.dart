import 'package:automapper_example/automapper.dart';

void main() {
  ExampleMapper mapper = ExampleMapper();

  // UserDto user = UserDto(id: 1, name: 'joe');

  // final result = mapper.convert<UserDto, User>(user);

  // print(result.name);
}
