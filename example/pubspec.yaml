name: automapper_example
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: ">=2.18.7 <3.0.0"

dependencies:
  automapper: ^0.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  automapper_generator: ^0.0.1
  build_runner: ^2.0.0
